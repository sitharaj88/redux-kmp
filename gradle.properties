# Kotlin
kotlin.code.style=official

# Android
android.useAndroidX=true
android.nonTransitiveRClass=true

# Memory
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8

# Parallel
org.gradle.parallel=true
org.gradle.caching=true

# Compose Multiplatform experimental targets
org.jetbrains.compose.experimental.jscanvas.enabled=true
org.jetbrains.compose.experimental.wasm.enabled=true

# Suppress expect/actual classes beta warning
kotlin.suppressGradlePluginWarnings=true

# NOTE: Node.js deprecation warnings (url.parse, punycode, glob)
# are from Kotlin/JS test infrastructure (karma-webpack) and
# will be fixed in future Kotlin versions. Safe to ignore.

# ============================================
# CUSTOMIZE THESE FOR YOUR LIBRARY
# ============================================
GROUP=in.sitharaj.reduxkmp
VERSION_NAME=1.0.0
POM_ARTIFACT_ID=redux-kmp
LIBRARY_NAME=Reduxkmp
LIBRARY_PACKAGE=in.sitharaj.reduxkmp
